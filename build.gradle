plugins {
    id 'java-library'
//    id("io.qameta.allure") version "2.17.0"
}

repositories {
    mavenCentral()
}

dependencies {
    testImplementation (
            "com.codeborne:selenide:6.19.1",
            "org.junit.jupiter:junit-jupiter:5.9.3"
//            'io.qameta.allure:allure-junit5:2.17.0' // Зависимость для вашего тестового фреймворка
            )
}

test {
    useJUnitPlatform()
}

//allure {
//    version = "3.0.0" // Используйте актуальную версию Allure Report
//    reportDir = file("build/allure-report")
//    resultsDir = file("build/allure-results") // Директория для сохранения результатов в формате JSON
//}
